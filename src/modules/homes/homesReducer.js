import api from '../../config/api'
import axios from 'axios'



const initialState = {
  data: null
}

// Actions


// Thunk actions

export default (state = initialState, action) => {
  switch (action.type) {
  default:
    return state
  }
}